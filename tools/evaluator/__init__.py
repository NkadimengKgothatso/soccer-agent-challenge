"""The submission contract, as much of it as a student needs.

Only `metadata` and `submissions` ship in the student distribution:
what team.toml must say, and what a submission folder may hold. Both
are copied unchanged from the platform, so a submission this accepts
is one the marker accepts.
"""
